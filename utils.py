import settings


def heightPercent(percentage):
    return (settings.HEIGHT / 100 * percentage)


def widthPercent(percentage):
    return settings.WIDTH / 100 * percentage


# Actually this thing has not mmuch usage lmao
